<%= link_to("Home", root_path, class: "navbar-brand") %>

<% if logged_in? %>
	<%= link_to("Log Out", session_path(session), class: "navbar-brand", method: 'delete') %>
<% else %>
	<%= link_to("Sign Up", new_user_path, class: "navbar-brand") %>
	<%= link_to("Sign In", new_session_path, class: "navbar-brand") %>
<% end %>

